[global]
flavor = "gfm"
disable = ["MD013"]

# CHANGELOG.md is generated by release-please and uses its own markdown
# conventions (`*` list markers, leading blank lines); don't fight it.
exclude = [
  ".git",
  "CHANGELOG.md",
  "dist",
  "tmp",
]
